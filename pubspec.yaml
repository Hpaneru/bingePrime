name: binge_prime
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  cloud_firestore:
  firebase_messaging:
  firebase_auth: ^0.16.0
  google_sign_in:
  firebase_analytics:
  mdi:
  firebase_core:


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
